# `features/worldinfo`

World Info (Lorebook) feature migration target.

Scope (initial):
- Vue shell + state model for settings & selection.
- No changes to legacy `public/scripts/world-info.js` yet.
- Provide a future integration point to replace the legacy drawer gradually.

